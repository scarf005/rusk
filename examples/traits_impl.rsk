pub trait Named
    fn name(&self) -> &str

pub struct User
    pub name: String

impl Named for User
    fn name(&self) -> &str = &self.name

pub fn greet(user: &impl Named) =
    do println!("hello {}", user.name())
