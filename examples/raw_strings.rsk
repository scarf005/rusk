pub fn pattern() -> &'static str =
    r#"^\d{4}-\d{2}-\d{2}$"#

pub fn contains_digit(value: &str) -> bool =
    value.chars().any(|character| character.is_ascii_digit())

pub fn main() =
    let _ = pattern()
    let _ = contains_digit("rusk1")
