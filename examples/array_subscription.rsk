pub fn pick(xs: &[i32], index: usize) -> i32 =
    let values = [10, 20, 30]
    let repeated = [index as i32; 3]
    let nested = [[1, 2], [3, 4]]
    let direct = xs[index]
    let literal = values[1]
    let matrix = nested[1][0]
    direct + literal + matrix + repeated[2]
