#![allow(dead_code)]

use std.collections.HashMap

pub fn counts() -> HashMap[String, i32] =
    let mut map = HashMap.new()
    do map.insert("one".to_string(), 1)
    do map.insert("two".to_string(), 2)
    map
